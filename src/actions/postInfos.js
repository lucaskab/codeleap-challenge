export const handleId = (data) => {
  return {
      type: 'handle_id',
      data,
  }
} 

export const handleTitle = (data) => {
  return {
      type: 'handle_title',
      data,
  }
} 

export const handleContent = (data) => {
  return {
      type: 'handle_content',
      data,
  }
} 